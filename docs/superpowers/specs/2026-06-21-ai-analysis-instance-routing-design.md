# AI 分析实例路由设计

## 目标

AI 分析获取数据库元数据时，应使用用户明确选择的实例，或遵循数据查询的自动路由逻辑。不得在实例已经确定后，再使用集群高可用域名覆盖该实例地址。

## 当前问题

- AI 分析接口只接收集群和数据库，没有接收 `route_mode`、`instance_id`。
- 后端使用 `pick_instance(..., for_change=False)` 初步选择查询实例。
- MySQL 元数据读取函数随后优先使用 `cluster.ha_domain`，导致前一步选出的实例失效。
- 普通分析、流式分析和外部 AI 分析存在相同风险。

## 交互设计

AI 分析页面复用数据查询页面的实例路由交互：

- MySQL 显示“自动 / 手动”选择。
- 自动模式不传具体实例，由后端按查询路由配置和实例角色自动选择。
- 手动模式显示当前集群实例列表，用户必须选择一个实例。
- MongoDB 当前保持自动模式，不新增手动选择控件。
- 切换数据库类型、业务范围或集群时，清空不再有效的实例选择。

## 请求与路由

页面向普通或流式 AI 分析接口提交：

```json
{
  "db_type": "mysql",
  "cluster_id": 1,
  "database": "example",
  "statement": "SELECT * FROM users",
  "route_mode": "manual",
  "instance_id": 12
}
```

后端处理规则：

1. 校验集群、权限、业务线和环境。
2. 读取 `route_mode`，仅接受 `auto`、`manual`。
3. 手动模式要求有效的 `instance_id`，且实例必须属于目标集群、数据库类型一致并处于启用状态。
4. 自动模式调用现有 `pick_instance`：
   - 优先使用集群配置的数据查询路由实例；
   - 未配置时按现有角色逻辑选择适合查询的实例。
5. 将最终实例传给元数据读取函数。

普通分析与流式分析共用同一实例解析方法，避免行为漂移。

外部 AI 分析接口当前没有实例选择页面，但同样使用自动查询路由逻辑；其元数据连接也不得使用高可用域名覆盖最终实例。

## 元数据连接

MySQL 和 MongoDB 元数据读取统一遵循：

- 主机地址使用最终实例的 `resolved_ip`，为空时使用 `host_input`。
- 端口、用户名和密码均使用该实例配置。
- `cluster.ha_domain` 不参与 AI 元数据连接。

高可用域名仍保留用于高可用状态、拓扑和切换相关功能，不改变这些功能的行为。

## 错误处理

- 手动模式未选择实例：返回 400。
- 实例不存在、不属于集群、类型不匹配或未启用：返回 400。
- 自动模式没有可用实例：返回 400。
- 元数据连接失败：保留现有分析失败响应，并包含底层错误信息。

## 测试

后端测试覆盖：

- 手动模式使用请求中的实例。
- 手动实例不属于集群时拒绝请求。
- 自动模式使用查询路由配置或自动角色选择结果。
- MySQL 元数据连接使用实例地址，即使集群配置了高可用域名。
- 普通、流式和外部 AI 分析使用一致的实例路由。

前端通过构建和组件编译验证：

- AI 分析表单包含自动/手动模式和实例字段。
- 手动模式提交 `instance_id`。
- 自动模式不携带残留实例 ID。

## 非目标

- 不改变数据查询和数据变更现有路由算法。
- 不调整高可用域名的拓扑、检测或切换逻辑。
- 不为 MongoDB 新增手动实例选择交互。
- 不修改 AI 提示词和分析模型调用逻辑。
